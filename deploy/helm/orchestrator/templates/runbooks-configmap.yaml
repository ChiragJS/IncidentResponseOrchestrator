apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-runbooks
  labels:
    app: ai-agent
data:
{{- range $path, $_ := .Files.Glob "runbooks/*.md" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
